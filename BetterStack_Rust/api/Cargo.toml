[package]
name = "api"
version = "0.1.0"
edition = "2024"

[dependencies]
poem = "3.1.12"
serde = {version = "1", features = ["derive"] }
tokio = { version = "1", features = ["full"] }
store = {path = "../store"}
dotenvy = "0.15"
