use crate::store::Store;

impl Store {
    pub fn create_website(&self, username:String, password:String) ->String{
        print!("create website called");
        "1".to_string()        
    }
    pub fn get_website(&self) ->(){

    }
}
